# Task T18 - Documentation Pass (README + Tests README)

Goal
- Refresh README and add tests documentation so usage and verification are clear.

Suggested Skill
- Use `$unfold-dev-structured` (Sonnet).
- Review with `$unfold-codex-reviewer`.

Scope
- Update `README.md`:
  - Features overview (simple bullets).
  - Installation via `settings.py` (scripts + urls).
  - Configuration options summary.
  - Testing section with commands.
  - Link to `tests/README.md`.
- Create `tests/README.md`:
  - Test app overview.
  - Pytest vs Playwright scope.
  - How to run both.

Workflow
- This task can be merged after review (no manual hold required).

Deliverables
- Updated `README.md`.
- New `tests/README.md`.

Acceptance Criteria
- Docs are concise, accurate, and aligned with current behavior.
- Links between README and tests README are correct.

Tests to run
- None required (doc-only).
